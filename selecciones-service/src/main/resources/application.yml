server:
  port: 8082

spring:
  application:
    name: selecciones-service

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: always
